// Local dependencies.
import { ContentAddNew } from './component';

/**
 * Content Add New container.
 * 
 * Currently, this component doesn't need any data from stores,
 * so the container just exports the component directly.
 */
export const ContentAddNewContainer = ContentAddNew;
