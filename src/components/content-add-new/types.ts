// External dependencies.
import { ReactElement } from 'react';

/**
 * Content Add New component props.
 */
export interface ContentAddNewProps {
  // Currently no props needed, but keeping the interface for future extensibility.
}

/**
 * Content Add New component type.
 */
export type ContentAddNewComponent = (props: ContentAddNewProps) => ReactElement;
